/*#include <stdio.h>
void swap(int* px, int* py); //px와 py가 가리키는 주소를 담아라

int main()
{
	int a = 100, b = 200;

	printf("a=%d b=%d\n", a, b);
	swap(&a, &b);           //a와 b의 주소를 보낸다.
	printf("a=%d b=%d\n", a, b);

	return 0;
}

void swap(int* px, int* py)
{
	int temp;

	temp = *px;
	*px = *py;
	*py = temp;
}*/